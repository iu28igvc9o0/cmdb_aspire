spring: 
  application:
    name: composite-mirror
  jmx:
    default-domain: composite
#  cloud:
#    config:
#      name: ms-composite
#      profile: ${ENV_TYPE:dev}
#      uri:  http://${ENV_CONFIG_IP:localhost}:${ENV_CONFIG_PORT:18888}
#      label: ${GIT_BRANCH:master}
#      failFast: true
